---
scope: PR #207 (8e88bb7d..6c9eb0d0)
diff: git diff 8e88bb7dc03ec8d57f0a1bf39a7d94a2a23657d3..6c9eb0d0df0a369c73c93265696bf0cc13877896
files_changed: 22
lines_changed: 103
lanes: correctness, contracts, tests, security, simplification, focus
reviewer: codex-cli
reviewed_at: 2026-09-14T08:40:45Z
---

> **Commit IDs updated.** This review ran against head `e806a5c1`. The branch's
> commit messages were later edited, which changed the IDs; the file trees are
> identical. The diff command above uses the new head.

## Findings

No findings in any lane.

Codex's per-lane result, verbatim:

> Correctness: No findings.
> Contracts & call sites: No findings; repository-wide searches found no stale references to renamed files.
> Tests: No findings; tree comparison detects mismatches, and GitHub's default shell exits on a failing check.
> Security: No findings.
> Simplification: No findings.
> Focus: No findings; all five subtree trees exactly match their latest squash commits. Argument order is correct. COMMIT defaults to HEAD, checking the merge commit on pull_request and the pushed commit on push. fetch-depth: 0 supplies sufficient history.

Focus lane given to the reviewer: each touched subtree must equal the tree of its
latest squash commit; the new CI job's use of `test/lint/git-subtree-check.sh`
(argument order, the COMMIT default, pull_request merge checkout versus push,
fetch depth, failure propagation); and every reference to a renamed file.

## Depth of this review

Codex CLI 0.153.4, read-only sandbox. The run took 60 seconds
(08:39:45Z to 08:40:45Z) and used 34,156 tokens. It ran 14 commands.

An empty result needs checking, so the reviewer's main claims were checked
against its transcript and then re-run by hand:

- **Subtree trees.** Codex looked up each squash commit's tree with
  `git log -1 --format='%H %T' --grep='^git-subtree-dir: <dir>/*$' 6c9eb0d0` and
  listed each directory with `git ls-tree -d 6c9eb0d0 <dir>`. The IDs match.
  Re-run by hand with `git rev-parse 6c9eb0d0:<dir>`:

  | Subtree | Squash tree | Head tree | Base (8e88bb7d) |
  | --- | --- | --- | --- |
  | `src/crypto/ctaes` | `1b6c31139a` | match | differed |
  | `src/secp256k1` | `24cb68b837` | match | differed |
  | `src/minisketch` | `a749309e1b` | match | differed |
  | `src/leveldb` | `bd49d3c600` | match | differed |
  | `src/crc32c` | `5a1b5f7188` | match | already matched |

- **Renamed paths.** Codex ran `git grep` over the whole tree at `6c9eb0d0` for
  both the `btq` and `bitcoin` forms. The only hits are the upstream `bitcoin`
  names inside `src/secp256k1`, which is correct. A separate search outside
  `src/secp256k1` found no references to either form.
- **CI invocation.** Codex read `test/lint/git-subtree-check.sh` and
  `.github/workflows/ci.yml` at `6c9eb0d0`. Independently: the script exits 1 on
  FAIL and 0 on GOOD (tested against the pre-restore commit), and the
  `subtree check` job passed on this PR in both the push and pull_request runs.

Other checks run beside the review:

- libsecp256k1 `make check`: 3 of 3 pass (`tests`, `exhaustive_tests`,
  `noverify_tests`).
- CI "Build btqd and run address tests": pass (12 minutes).
- `actionlint` is not installed, so the workflow was not linted beyond a YAML
  parse.

## Disposition

No reviewer findings to disposition.

Out of scope, already tracked, not caused by this PR:

- "test each commit" fails on this PR, as it does on every multi-commit PR —
  PRE-EXISTING, #203.
- Functional tests not run; 126 already fail on master — PRE-EXISTING, #104.
